spring:
  datasource:
    url: jdbc:mysql://localhost:3306/springboot_testdb
    username: root
    password:

  jpa:
    hibernate:
      ddl-auto: update
      naming:
        physical-strategy: org.hibernate.boot.model.naming.PhysicalNamingStrategyStandardImpl